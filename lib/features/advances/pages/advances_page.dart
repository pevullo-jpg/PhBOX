import 'package:flutter/material.dart';

class AdvancesPage extends StatelessWidget {
  const AdvancesPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Anticipi', style: TextStyle(color: Colors.white)),
    );
  }
}
